"use client";

import React from 'react';

/**
 * Promotion scores removed: component no longer fetches or displays score (saves API/DB calls).
 */
export default function TourPromotionScore() {
  return null;
}

